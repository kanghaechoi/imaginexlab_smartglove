codeInitialize;
path = "/dairy/"
MAXFEATURES = 26;
NUMBEROFITERATION = 5;
for NumberFeature = 1:26
    for loop = 1:NUMBEROFITERATION
        DairyName = strcat('LSTM_', str(NumberFeature,"_", loop, '.log');
        fid = fopen( path + DairyName, 'wt' );
        fclose(fid);
        diary( path + DairyName	)
        smartgloveRuntime(3,NumberFeature)
        dairy off
    end
end